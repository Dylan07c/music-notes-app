export class CategoryCardModel{
    img: string;
    heading: string;
    body: string;

    constructor(img:string, heading:string, body:string) {
    this.img = img;
    this.heading = heading;
    this.body = body;
    }
}